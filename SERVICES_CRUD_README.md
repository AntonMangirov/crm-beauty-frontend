# CRUD для услуг - Документация

## 🎯 Обзор

Реализован полнофункциональный CRUD интерфейс для управления услугами салона красоты с использованием mock API и localStorage.

## 🏗️ Архитектура

### Типы и интерфейсы (`src/types/service.ts`)

- `Service` - основная модель услуги
- `CreateServiceRequest` - данные для создания
- `UpdateServiceRequest` - данные для обновления
- `ApiError` - типы ошибок
- `LoadingState` - состояния загрузки

### Mock API (`src/services/mockServiceApi.ts`)

- Полная имитация REST API
- Работа с localStorage для персистентности
- Имитация асинхронных вызовов
- Тестовые данные по умолчанию

### Хуки (`src/hooks/`)

- `useServices` - работа со списком услуг
- `useService` - работа с отдельной услугой
- Управление состояниями загрузки и ошибок

### Компоненты (`src/components/`)

- `ServiceCard` - карточка услуги с действиями
- `ServiceForm` - форма создания/редактирования
- `DeleteConfirmDialog` - подтверждение удаления
- `ServicesFilter` - поиск и фильтрация
- `Navigation` - навигация по приложению

## 🚀 Функциональность

### ✅ CRUD операции

- **Create** - создание новых услуг
- **Read** - просмотр списка услуг
- **Update** - редактирование существующих услуг
- **Delete** - удаление услуг

### 🔍 Поиск и фильтрация

- Поиск по названию и описанию
- Фильтрация по статусу (активные/неактивные)
- Очистка фильтров

### 🎨 UI/UX

- Адаптивная сетка карточек
- Модальные окна для форм
- Подтверждение удаления
- Индикаторы загрузки
- Обработка ошибок

## 📱 Страницы

### `/services` - Управление услугами

- Основная страница CRUD операций
- Список услуг в виде карточек
- Поиск и фильтрация
- Статистика услуг

### `/test` - Тестирование API

- Тестирование mock API
- Инициализация тестовых данных
- Проверка localStorage

## 🛠️ Использование

### Запуск приложения

```bash
npm run dev
```

### Навигация

- **Главная** (`/`) - лендинг страница
- **Услуги** (`/services`) - управление услугами
- **Тест API** (`/test`) - тестирование mock API

### Основные действия

1. **Создание услуги** - кнопка "Добавить услугу"
2. **Редактирование** - кнопка "Редактировать" на карточке
3. **Удаление** - кнопка "Удалить" с подтверждением
4. **Активация/деактивация** - переключатель статуса

## 🔧 Технические детали

### Mock API методы

- `getServices()` - получение всех услуг
- `getServiceById(id)` - получение по ID
- `createService(data)` - создание услуги
- `updateService(id, data)` - обновление
- `deleteService(id)` - удаление
- `searchServices(query)` - поиск
- `filterServices(isActive)` - фильтрация

### Локальное хранение

- Ключ: `crm_services`
- Автоматическая инициализация тестовыми данными
- Персистентность между сессиями

### Валидация форм

- Обязательные поля: название, цена, длительность
- Ограничения: длина названия, диапазон цен, время
- Валидация в реальном времени

## 🧪 Тестирование

### Тестовые данные

При первом запуске создаются 3 тестовые услуги:

- Классический маникюр (1500₽, 60 мин)
- Маникюр + гель-лак (2500₽, 90 мин)
- Педикюр (2000₽, 90 мин)

### Тестирование API

Используйте страницу `/test` для:

- Запуска полного теста API
- Инициализации тестовых данных
- Проверки работы localStorage

## 🔄 Переход на реальный API

Для перехода на реальный API:

1. Замените `mockServiceApi` на реальные HTTP вызовы
2. Обновите базовый URL в конфигурации
3. Добавьте обработку авторизации
4. Настройте обработку ошибок сети

## 📋 TODO

- [ ] Добавить авторизацию
- [ ] Реализовать загрузку изображений
- [ ] Добавить категории услуг
- [ ] Реализовать сортировку
- [ ] Добавить экспорт/импорт данных
















